version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - bun install
    build:
      commands:
        - bun run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
